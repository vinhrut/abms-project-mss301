server:
  port: 8082

spring:
  application:
    name: apartment-service
  datasource:
    url: jdbc:postgresql://localhost:5433/abms_db
    username: admin
    password: password123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: false