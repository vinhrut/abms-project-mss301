export function ReportExportActions({ report, exporting, onExport }) { return <div className="hero-panel__actions"><button className="btn btn-secondary" disabled={!report || exporting === 'PDF'} onClick={() => onExport('PDF')}>{exporting === 'PDF' ? 'Đang xuất PDF...' : 'Xuất PDF'}</button><button className="btn btn-primary" disabled={!report || exporting === 'XLSX'} onClick={() => onExport('XLSX')}>{exporting === 'XLSX' ? 'Đang xuất Excel...' : 'Xuất Excel'}</button></div> }
